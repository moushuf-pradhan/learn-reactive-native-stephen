import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

export const TextScreen = function () {
	return <Text>Text Screen</Text>;
};

const styles = StyleSheet.create({});

export default TextScreen;
